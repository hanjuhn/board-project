name: Backend CI/CD

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      # 현재 버전 읽기
      - name: Read CURRENT version
        id: version
        run: |
          FILE="k8s/backend-deployment.yaml"
          CURRENT=$(grep "image: hanjuhn/backend:" $FILE | sed 's/.*backend:\(.*\)$/\1/')
          if [ -z "$CURRENT" ]; then CURRENT="v0"; fi
          echo "current_version=$CURRENT" >> $GITHUB_OUTPUT

      # 다음 버전 계산
      - name: Calculate NEXT version
        id: next
        run: |
          CUR="${{ steps.version.outputs.current_version }}"
          NUM=$(echo $CUR | sed 's/v//')
          NEXT=$((NUM+1))
          echo "next_version=v$NEXT" >> $GITHUB_OUTPUT

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # backend 이미지 빌드 + 푸시
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          platforms: linux/amd64
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/backend:${{ steps.next.outputs.next_version }}

      # K8S yaml 자동 업데이트
      - name: Update K8S Deployment YAML
        run: |
          sed -i "s|image: hanjuhn/backend:.*|image: hanjuhn/backend:${{ steps.next.outputs.next_version }}|" k8s/backend-deployment.yaml

      # 업데이트된 파일 커밋
      - name: Commit updated YAML
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add k8s/backend-deployment.yaml
          git commit -m "Update backend to ${{ steps.next.outputs.next_version }}"
          git push origin main
